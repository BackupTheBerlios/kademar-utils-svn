post_install() {
    if [ "$LANG" = C ]; then
        LANG=es_ES.UTF-8  rahisi-config-update-apps 
    else    
        rahisi-config-update-apps
    fi
}

post_remove(){
    rm -fr /usr/share/helioxhelper
}