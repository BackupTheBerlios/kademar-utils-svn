# Maintainer: Adonay Jonay Sanz Alsina <adonay@kademar.org>
pkgname=kademar-meta-common-utils
pkgver=1.5
pkgrel=1
epoch=
pkgdesc="kademar Metapackage Common Utils"
arch=(any)
url="http://www.kademar.org"
license=('GPL')
groups=()
depends=('kademar-meta-xorg' 'kademar-meta-network-management' 'kademar-meta-soundserver' 'kademar-meta-printserver-cups'  'cadi-svn' 'hunspell-ca' 'hunspell-es' 'hunspell-en' 'hunspell-fr' 'xdg-utils' 'xdg-user-dirs' 'python2-pyqt' 'os-prober' 'packer' 'systemd-ui')
makedepends=()
checkdepends=()
optdepends=('audioconversor-svn: Multiformats video and music conversor'
'css-miami-svn: Web CSS WYSIWIG Creator')
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
#source=($pkgname-$pkgver.tar.gz)
noextract=()
#md5sums=('a1be535e26c780470e3c599b44e7d6bc') #generate with 'makepkg -g'

build() {
  cd "$srcdir"
}

# package() {
#   mkdir -p "$pkgdir/usr/bin"
#   cp $srcdir/apt* $srcdir/dpkg "$pkgdir/usr/bin"
#   find "$pkgdir"/usr/ -type f -exec chmod 644 "{}" \;
#   find "$pkgdir"/usr/ -type d -exec chmod 755 "{}" \;
#   find "$pkgdir"/usr/bin -type f -exec chmod 755 "{}" \;
# }

# vim:set ts=2 sw=2 et:
