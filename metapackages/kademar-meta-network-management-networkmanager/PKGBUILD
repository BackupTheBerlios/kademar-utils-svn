# Maintainer: Adonay Jonay Sanz Alsina <adonay@kademar.org>
pkgname=kademar-meta-network-management-networkmanager
pkgver=1.1
pkgrel=1
epoch=
pkgdesc="kademar Metapackage NetworkManager as network management"
arch=(any)
url="http://www.kademar.org"
license=('GPL')
groups=()
depends=('kademar-meta-common-utils' 'networkmanager' 'modemmanager' 'mobile-broadband-provider-info' 'networkmanager-dispatcher-openntpd' 'networkmanager-dispatcher-sshd' 'networkmanager-dispatcher-netfs' 'network-manager-applet' 'networkmanager-openconnect' 'networkmanager-openvpn' 'networkmanager-pptp' 'networkmanager-vpnc' 'dnsmasq' 'bluez' 'iptables' 'usb_modeswitch')
makedepends=()
checkdepends=()
optdepends=()
provides=('kademar-meta-network-management')
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
#source=($pkgname-$pkgver.tar.gz)
noextract=()
#md5sums=('a1be535e26c780470e3c599b44e7d6bc') #generate with 'makepkg -g'

build() {
  cd "$srcdir"
}

# package() {
#   mkdir -p "$pkgdir/usr/bin"
#   cp $srcdir/apt* $srcdir/dpkg "$pkgdir/usr/bin"
#   find "$pkgdir"/usr/ -type f -exec chmod 644 "{}" \;
#   find "$pkgdir"/usr/ -type d -exec chmod 755 "{}" \;
#   find "$pkgdir"/usr/bin -type f -exec chmod 755 "{}" \;
# }

# vim:set ts=2 sw=2 et:
